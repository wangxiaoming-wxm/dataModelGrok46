# 数据深度分析报告(融合版)

> 本文融合 5 份独立子 agent 深度分析报告(单变量特征工程 / 相关性信息熵 / 交叉交互 / 数据结构生成机制 / 独立审核补充),并经主会话交叉复核。
> 所有数字均为本地 Python(pandas/numpy/scipy/sklearn/lightgbm/catboost)实算,可复核。
> 数据:`/Volumes/pssd/app/ml/正式比赛/data/train.csv`(14930×45,含 label)、`test.csv`(6398×44)。
> 二分类,AUC 评测,正例率 0.1002(1496/14930)。

---

## 一、数据概览

### 1.1 基本统计

| 项 | 值 |
|---|---|
| 训练集 | 14930 行 × 45 列(含 label) |
| 测试集 | 6398 行 × 44 列(无 label) |
| 正例率 | 0.1002(1496 / 14930) |
| 缺失 | 仅 condition(train 144 条 0.96% / test 65 条 1.02%),其余 43 列零缺失 |
| 评测指标 | AUC |

### 1.2 45 列的功能分块

| 功能块 | 列 | 角色 |
|---|---|---|
| 标识 | id | 16 位 hex 随机标识(不可预测) |
| 车型分组 | source | 生成分组根变量(11 车型) |
| 引擎规格 | V / cc / max_g / t3 / code / grades / x19 | 由 source 决定的确定性属性 |
| 二值标签 | t1 / t2 / w1 / w2 / c1 / c2 / r1 / r2 | 相互冗余/互补的 0/1 列 |
| 观测噪声 | x0~x17 / x18 / x20 / condition / livability / age_range / region | source 均值 + 独立噪声 |
| 时间/批次 | month / days / version | 采集批次 / 车龄 / 数据版本 |

---

## 二、数据生成机制与因果链(最底层结构)

逆向 45 列,推断出如下因果层级(有实测数字支撑):

```
source (车型 = CAR_{0..10} | ENG,根节点,11 个取值)
   │
   ├─ 确定性派生(100% 由 source 决定):
   │    code(4类)、t3_letter(M/P/E)、car、eng
   ├─ 近似确定性派生:
   │    grades(92.4%)、主导 month(75.3%)、V、cc、max_g、x19(=V²)、x18 振幅
   ├─ source 签名列(组间方差占比 0.64~1.0,即"source 均值 + 独立噪声"):
   │    x1 / x5 / x9 / x10 / x11 / x13 / x14 / x15 / x17 / x16
   ├─ 纯噪声列(组间方差占比 <0.18):
   │    x2 / x4 / x6 / x7 / x12 / x20
   │    x18 = k·Uniform(-1,1),k∈{1..5} 按 source 变化
   └─ 独立观察量(不依赖 source,是真正的信号源):
        days(车龄)、condition(车况)、region(20类)、version(19类)、age_range、livability
```

### 2.1 关键派生关系的量化证据

| 关系 | 实测证据 |
|---|---|
| **x19 = V² 的确定性函数** | corr(V²,x19)=0.999998;x19=round(V,2)²(四舍五入)精确匹配 99.03%,145 行差异是 V 末位 .xx5 的舍入模式差异;x19 仅 16 个唯一值 |
| **x18 是异方差纯噪声** | 组间方差占比 ≈0;每个 source 内近似 Uniform(-a,a),振幅 a∈{1..5}(CAR_1=1.0 到 CAR_6=5.0);峰度 -1.2 恰为 Uniform 理论峰度;与 label 相关 -0.004 |
| **V≈cc≈max_g 同一物理量** | V↔cc 相关 0.939,V↔max_g 0.904,cc↔max_g 0.772 |
| **t≈w、r≈1−c** | t1+t2=1 占 90.6%;t1+t2==w1+w2 占 82.6%;r1≈1−c1 占 81.2% |
| **livability ≈ region 的连续化** | 与 region 几乎 1:1 映射 |

### 2.2 source(车型)结构

- 11 个车型 CAR↔ENG 严格 1:1 双射:`CAR_0→709, CAR_1→591, CAR_2→262, CAR_3→467, CAR_4→899, CAR_5→062, CAR_6→331, CAR_7→966, CAR_8→843, CAR_9→811, CAR_10→651`。
- 样本量 89(CAR_8)~3709(CAR_1),正例率 5.6%~15.6%。
- 每个 source 的 V 是 28~31 个以 0.0001 为步长的离散值;每个 source 有"主导 month"(93%~96% 样本集中)。

| source | 样本量 | 正例率 |
|---|---:|---:|
| CAR_1\|ENG_591 | 3709 | 0.0919 |
| CAR_0\|ENG_709 | 3602 | 0.1030 |
| CAR_2\|ENG_262 | 3586 | 0.1088 |
| CAR_5\|ENG_062 | 1024 | 0.0801 |
| CAR_3\|ENG_467 | 760 | 0.1092 |
| CAR_4\|ENG_899 | 613 | 0.0946 |
| CAR_6\|ENG_331 | 548 | 0.0839 |
| CAR_9\|ENG_811 | 398 | 0.1080 |
| CAR_7\|ENG_966 | 313 | 0.1022 |
| CAR_10\|ENG_651 | 288 | 0.1562 |
| CAR_8\|ENG_843 | 89 | 0.0562 |

### 2.3 month 与 days 的时间结构

- **month(M0~M12)是"采集批次"而非日历月**:M2 占 61.5%(9184/14930),M1 占 23.0%;每个车型 93%~96% 样本集中在一个 month。
- **days(24~11781)是车龄**:独立于 source(组内/全局 std 比 0.997),近均匀分布,是唯一独立于车型的连续信号源。
- version(v1~v19)是数据采集版本/批次,v1=5731/v2=3353/v3=1730,无信号(单变量 AUC 0.503)。

---

## 三、单变量信号分析

### 3.1 诚实 AUC 口径说明

本文所有 AUC 区分三种口径,避免混淆:

| 口径 | 适用列 | 特点 |
|---|---|---|
| 原始值双向取 max | 连续列 | 不编码、无过拟合;`max(AUC(x), AUC(-x))` 自动对齐方向 |
| 折内均值编码 | 类别列 | StratifiedKFold(5折,seed=42)折内 target encoding,避免全量编码虚高 |
| q10 分箱折内编码 | 连续列(交互用) | 先等频分 10 箱再折内编码,略保守(分箱损失) |

**重要**:condition 的 AUC 有两个口径,都正确但方向不同:
- 单向 AUC = 0.4676(<0.5,即 condition 越高风险越低)
- 双向取 max = 0.5324(取负向后)

### 3.2 原始列单变量 AUC 排名(诚实)

| 列 | 诚实 AUC | 类型 | 备注 |
|---|---:|---|---|
| **days** | **0.5932** | 连续 | 最强原始列,单调上升 |
| **region** | **0.5385** | 类别(折内) | 最强类别列 |
| condition | 0.5324 | 连续(双向) | U 形,见 §六 |
| x1 | 0.5282 | 连续 | source 签名列 |
| x5 | 0.5247 | 连续 | source 签名列 |
| age_range | 0.5229 | 序数 | 右尾高危 |
| t3 | 0.5225 | 类别(折内) | 拆分后数字/字母更弱 |
| source | 0.5216 | 类别(折内) | 车型 |
| V | 0.5205 | 连续 | 离散多簇 |
| x19 | 0.5193 | 连续 | =V²,仅 16 值 |
| x14 / x0 / x17 / x9 | 0.5164~0.5183 | 连续 | source 签名 |
| cc / x2 / x15 / x13 | 0.5136~0.5152 | 连续 | 弱 |
| max_g / x20 / x7 | 0.5104~0.5118 | 连续 | 弱 |
| 二值列 t1/t2/r1/r2/c1/c2/w1/w2 | 0.5008~0.5184 | 二值 | 几乎无独立信号 |
| x18 / x6 / x16 / x3 / x4 / x12 | 0.5011~0.5055 | 连续 | 噪声 |
| month / grades / version / code | 0.5004~0.5058 | 类别(折内) | 基本噪声 |

### 3.3 最强衍生特征(比值族)

对 days 与 condition 的交互做比值化,得到的衍生特征显著强于 days 单独:

| 衍生特征 | 公式 | 原始值双向 AUC |
|---|---:|---:|
| rate | days × (1 − rank(condition\|source)) | 0.6207 |
| ratio | days / (condition / median(condition\|source)) | 0.6204 |
| days/sqrt(condition) | days / √condition | 0.6108 |
| days/condition | days / condition | 0.6043 |
| days | (基线) | 0.5932 |

- 指数扫描 `days / condition^p` 显示 **p=0.5 最优**(即除以 sqrt(condition) 优于除以 condition 本身)。
- 等价形式 `log(days) − 0.5·log(condition)` AUC 完全相同(单调变换等价)。
- 物理含义:风险 ∝ 车龄 / √车况,condition 作为 days 的"尺度分母"把 days 的 AUC 从 0.593 抬到 0.611~0.621。

---

## 四、相关性与冗余结构

### 4.1 高相关簇

| 特征对 | Pearson r |
|---|---|
| V ↔ x19 | 0.9967 |
| cc ↔ V | 0.9391 |
| cc ↔ x19 | 0.9279 |
| x19 ↔ max_g | 0.9244 |
| V ↔ max_g | 0.9040 |

### 4.2 冗余簇(层次聚类)

|Spearman|≥0.5 得到一个 13 列"排量/动力 + 车型签名"冗余簇:
`{cc, V, t3_num, x1, x5, x9, x10, x11, x13, x15, x17, x19, max_g}`

### 4.3 x0~x18 的 source 决定程度(加权 ANOVA,组间方差占比)

| 列 | 组间占比 | 判读 |
|---|---:|---|
| x19 | 1.0000 | 完全由 source 决定(=V²) |
| x1 / x9 / x5 / x17 / x13 | 0.864~0.901 | 车型签名 |
| x14 / x11 / x10 / x15 | 0.753~0.787 | 车型签名 |
| x16 | 0.6439 | 几乎只标 CAR_8(组均值 -0.505) |
| x3 / x0 | 0.517~0.547 | 部分车型相关 |
| x8 | 0.3662 | 弱车型相关 |
| x6 / x7 / x2 / x4 | 0.090~0.179 | 基本独立 |
| x20 | 0.0594 | 独立(与 condition 相关) |
| x12 | 0.0294 | 独立 |
| x18 | -0.0001 | 纯噪声(组间方差≈0) |

---

## 五、信息熵与派生关系

### 5.1 条件熵 H(X|source) 揭示的派生关系

| 列 | H(X) | H(X\|source) | 冗余度 MI/H | 判读 |
|---|---:|---:|---:|---|
| code | 1.6867 | 0.0000 | 1.0000 | 100% 由 source 决定 |
| t3_letter | 1.1093 | 0.0000 | 1.0000 | 100% 由 source 决定 |
| grades | 1.0320 | 0.0780 | 0.9244 | 92.4% 由 source 决定 |
| month | 1.7737 | 0.4379 | 0.7531 | 75.3% 由 source 决定 |
| region | 3.6062 | 3.4672 | 0.0386 | 基本独立(仅 3.9% 冗余) |
| version | 2.9737 | 2.9626 | 0.0038 | 完全独立(0.4% 冗余) |

参考:H(source)=2.7465 bits,H(label)=0.4696 bits。

### 5.2 结论

- code、t3_letter 是 source 的完全确定性函数,不含独立信息。
- grades 92.4%、month 75.3% 由 source 决定。
- **region、version 几乎完全独立于 source**,是独立观察量。

### 5.3 互信息(MI)与 AUC 的异常列

| 类型 | 列 | 说明 |
|---|---|---|
| MI 高但 AUC 低(非线性) | condition(MI 第3 但单向 AUC 0.468,U形)、x20、x9(驼峰) | 需分箱/分段处理 |
| MI 低但 AUC 高 | x1/x5/x10(单调但 kNN-MI 低估) | source 常量型,label 信息在组间,kNN 组内找不到梯度 |

---

## 六、非线性关系

### 6.1 days 的"悬崖"

days 在 **700~880 区间出现局部低风险坑**(正例率 0.033 vs 基线 0.10),最底 750~775 仅 0.012。很窄的局部异常,指示符单变量 AUC 仅 0.529(覆盖样本少)。

### 6.2 condition 的 U 形

condition 十分位正例率:**最低 10%(0.147)→ 中间(0.088~0.113)→ 最高 10%(0.066)**。即 condition 极小(车况差)风险高、极大风险低,但极低端是局部高点,整体呈 U 形,非单调。

### 6.3 age_range 的右尾

age_range≥8 正例率 0.166(n=175),是独立高危子群;age_range=10 达 0.333(仅 3 例,噪声)。

### 6.4 强信号子群(样本量充足)

| 特征取值 | 正例率 | 样本量 |
|---|---:|---:|
| source=CAR_10 | 15.62% | 288 |
| condition<0.05 | 15.68% | 676 |
| age_range=8 | 16.46% | 158 |
| region=f09d | 11.91% | 3560 |
| region=c1f5 | 3.72% | 188 |
| source=CAR_8 | 5.62% | 89 |

---

## 七、交叉特征与交互效应

方法:StratifiedKFold(5折)折内目标编码(m=20 平滑)算 AUC,多 seed 验证稳定性。

### 7.1 真正有价值的交叉(排除 proxy 假象)

| 交叉 | 折内增益 | 组合 AUC | 稳定性 |
|---|---:|---:|---|
| **condition × source** | **+0.0412** | 0.5947 | 稳定(std 0.002) |
| days × condition | +0.0239 | 0.6189 | 稳定 |
| source × cond_r | +0.0224 | 0.5849 | 稳定 |
| condition × ratio | +0.0207 | 0.6346 | 稳定 |
| region × age_range | +0.0183 | 0.5585 | 稳定 |
| condition × region | +0.0133 | 0.5667 | 稳定 |
| condition × grades | +0.0106 | 0.5640 | 稳定 |
| days × region | +0.0074 | — | 弱但真实 |
| days × source | +0.0048 | — | 弱但真实 |

### 7.2 proxy 假增益(已证伪)

`condition×code`(+0.039)、`condition×cc`(+0.032)、`condition×max_g`(+0.026)、`condition×V`(+0.023) 看似高,但都是 condition 的带噪副本(cc/max_g/V 与 condition 相关 0.29~0.52),组合键本质是"更细的 condition 分箱",非真交互。

### 7.3 条件效应异质性(condition×source 的物理来源)

| source | corr(condition) | condition 折内 AUC |
|---|---:|---:|
| CAR_1 | -0.104 | 0.654 |
| CAR_10 | -0.136 | 0.608 |
| CAR_6 | -0.079 | 0.581 |
| CAR_8 | -0.031 | 0.576 |
| CAR_2 | -0.053 | 0.549 |
| CAR_4 | -0.041 | 0.536 |
| CAR_0 | -0.041 | 0.534 |
| CAR_5 | -0.030 | 0.519 |
| CAR_9 | -0.021 | 0.518 |
| CAR_3 | -0.021 | 0.516 |
| CAR_7 | -0.002 | 0.471 |

condition 效应在车型间严重异质:CAR_1 强负相关(-0.104),CAR_7 几乎无效应(-0.002),折内 AUC 相差 3 倍。

days 效应同样异质:单 source 内 days 相关从 CAR_1 的 0.023 到 CAR_5 的 0.175、CAR_10 的 0.151,相差 7.6 倍;单 region 内甚至符号翻转(e1d5=+0.243、abb2=-0.080)。

### 7.4 高阶交互

唯一有增量的三阶:`days_q10 × cond_q10 × source`(+0.0085 vs 最优二阶 days×condition)。其余三阶/四阶全部反降(稀疏化吃掉增益)。

---

## 八、train/test 分布漂移与泄漏检查

### 8.1 漂移检测

| 检验 | 结果 | 结论 |
|---|---|---|
| 数值列 PSI | 最大 0.0053(x20),其余 <0.004 | 几乎无漂移 |
| 数值列 KS | 仅 x11/x12 略低于 0.05 但统计量极小(0.022) | 非实质漂移 |
| 类别列卡方 | month p=0.996,source p=0.882,region p=0.862,version p=0.168 | 无显著漂移 |
| id 顺序结构 | 排序后 train/test 交替 run 数 8888 ≈ 随机 8957 | 无时间/批次顺序 |
| 对抗验证 | 线性 LogisticRegression AUC=0.503(随机);LGB 0.554 主要来自 version 的微弱可检测漂移 | version 有 ~0.05 微弱漂移,可忽略 |

### 8.2 切分结构

train/test 是**干净随机切分**(非分组/时间切分):
- source 各车型 train 占比 0.685~0.722(期望 0.70),χ² p=0.9988。
- days 无时间断点(KS p=0.606,十分位 train 占比恒定 0.69~0.71)。
- 无任何车型/月份只出现在 test。

### 8.3 泄漏检查

| 检查项 | 结果 |
|---|---|
| id 均匀性 | nibble/byte 均匀(χ² p=0.65/0.49),16 位置熵 3.999(满载),字节两两独立(p>0.13) |
| id 与 label | 8 字节 RF→label OOF AUC=0.4868(无信号);完整 id target-encoding OOF=0.5000 |
| id 与 source | RF balanced_acc=0.0936 ≈ 随机 0.0909 |
| train/test id 交集 | 0,无可链接规则 |
| 全特征 OOF | LightGBM 5折 = 0.622(真实信号,非泄漏);无单列/组合接近 0.9 |
| 伪标签 | 遍历 id 子串仅 9 个"纯负"组(count≤58,小样本随机) |

---

## 九、不同建模配置下的 OOF 水平(客观记录)

本节记录不同特征集 × 模型 × 集成配置下实测的本地 OOF,供对比参考(数字均为实测,不同配置不可直接横向比较):

### 9.1 原始列直接喂单模型(无手工交叉特征)

| 配置 | 本地 OOF |
|---|---|
| LightGBM 仅 days+condition+source+region | 0.6565 |
| LightGBM core10(days/condition/source/region/age_range/cc/V/max_g/x20) | 0.6639 |
| LightGBM 全 43 列 | 0.6309 |
| CatBoost 全 43 列(原生有序目标编码) | 0.658 |
| 全特征 LightGBM 5折(agent4 口径) | 0.622 |

注:不同 agent 因超参/seed 数不同,LightGBM 全特征 OOF 在 0.622~0.631 之间,同一量级。

### 9.2 加入手工交叉特征 + CatBoost 集成(已提交验证)

| 方案 | 本地 OOF | 线上 AUC |
|---|---:|---:|
| W62(0.62·rank(main)+0.38·rank(alt)) | 0.70159 | 0.71503 |
| vz17(双臂+byte0) | 0.70170 | 0.71487 |

这两档配置的关键差异:
- 9.1 是"原始列直接喂单模型、少量 seed、无手工交叉"。
- 9.2 是"80+ 手工类别交叉特征 + 双编码世界(cond_r/rate)+ CatBoost + 10fold + 8seed + 3bagging + 双臂 rank 融合"。

即:在 9.1 配置下 OOF 约为 0.63~0.66;在 9.2 配置下 OOF 约为 0.70。差异主要来自交叉特征工程与集成的强度,而非数据信号本身的变化。

---

## 十、关键数字速查表

| 项 | 数值 |
|---|---|
| 正例率 | 0.1002 |
| 最强原始列 days | AUC 0.5932 |
| 最强类别列 region | AUC 0.5385(折内) |
| 最强衍生特征 ratio/rate | AUC 0.6204 / 0.6207 |
| 最强交互 condition×source | 折内增益 +0.0412 |
| x19 与 V 的关系 | x19=round(V,2)²,corr 0.999998 |
| x18 性质 | k·Uniform(-1,1),k∈{1..5},纯噪声 |
| 冗余簇 | 13 列(排量/动力+车型签名) |
| train/test 漂移 | 最大 PSI 0.0053,干净随机切分 |
| id 性质 | 纯随机 64 位,无泄漏,不可预测 |
| 正例率最高 source | CAR_10 15.6% |
| 正例率最低 source | CAR_8 5.6% |
| 正例率最高 region | f09d 11.9% |
| 正例率最低 region | c1f5 3.7% |

---

## 附:各子 agent 报告对照与口径说明

| 报告 | 主题 | 关键贡献 |
|---|---|---|
| agent1_features.md | 单变量 + 特征工程 | 完整 43 列 AUC 表、分箱、比值族发现 |
| agent2_correlation_entropy.md | 相关性/熵/MI | 冗余簇、派生关系、MI vs AUC 异常列 |
| agent3_interactions.md | 交叉/交互 | condition×source 最强交互、proxy 假象识别 |
| agent4_structure_generation.md | 结构/生成/泄漏 | id 逆向、source 车型结构、x19=x²/x18 噪声 |
| agent5_review_supplement.md | 独立审核 + 补充 | 切分结构验证、feature importance 陷阱、CatBoost 原生编码增益 |

### 口径差异说明(重要,避免混淆)

1. **condition 的 AUC**:单向 0.4676(负相关)与双向取 max 0.5324 是两个口径,都正确。本文正文统一用双向取 max。
2. **比值特征的 AUC**:原始值双向取 max(约 0.611~0.621)略高于 q10 分箱折内编码(约 0.61),差异来自分箱损失。
3. **LightGBM 全特征 OOF**:不同 agent 因超参/seed 不同报 0.622 与 0.631,同一量级。
4. **source 的 AUC**:折内均值编码 0.5216;若用序数编码会低估至 0.4917(信息在组间,序数编码丢失)。

### 各 agent 结论的交叉验证状态

| 结论 | 复核结果 |
|---|---|
| ratio=days/cond_r 是强特征(AUC 0.620) | ✅ 多 agent 一致 + 主会话复核 |
| condition×source 最强交互(+0.041) | ✅ 一致 |
| x19=V²、x18=纯噪声 | ✅ 一致(多个 agent 独立确认) |
| id 纯随机、无泄漏 | ✅ 一致 |
| train/test 干净随机切分 | ✅ 一致 |
| region 独立于 source | ✅ 一致 |

---

*原始数据:`/Volumes/pssd/app/ml/正式比赛/data/`
*各 agent 原始报告:`/Volumes/pssd/app/ml/正式比赛/20280811-zcode/analysis_deep/`
*生成时间:2026-08-13*
