# 建模版本演进经验总结

> 本文记录一路建模过程中各版本的做法、本地 OOF、线上得分与遇到的问题点。
> 只做客观记录,所有数字均来自实测或已提交的线上结果,可复核。

---

## 一、任务与数据

| 项 | 值 |
|---|---|
| 任务 | 保险理赔预测,二分类 |
| 评测指标 | AUC |
| 训练集 | 14930 行 × 45 列(含 label) |
| 测试集 | 6398 行 × 44 列(无 label) |
| 正例率 | 0.1002(1496 / 14930) |
| 数据位置 | `/Volumes/pssd/app/ml/正式比赛/data/` |
| 约束 | 不过拟合、不作弊;不用外部数据/test 标签/伪标签;折内特征工程 |

---

## 二、已提交版本线上分数总览(按分数降序)

| 方案 | 本地 OOF | 线上 AUC | OOF→online gap | 说明 |
|---|---:|---:|---:|---|
| **W62** | 0.70159 | **0.71503** | +0.01344 | 加权融合,当前最高已提交 |
| **vz17** | 0.70170 | **0.71487** | +0.01317 | 双臂+byte0,最高可独立复现 |
| best_v1 (max2) | 0.70128 | 0.71453 | +0.01325 | 参考解 v1,双编码世界 max2 |
| vz13 | 0.70050 | 0.71393 | +0.01343 | 自训双臂,rank_w0.62 |
| **vz19** | 0.70355 | **0.71298** | +0.00943 | max2+byte0/7,OOF 最高但线上最低 |
| v4_max3 | ~0.703(含 ES 乐观) | 0.71222 | — | 旧体系三臂 max |
| v3_max3 | ~0.701 | 0.71184 | — | 旧体系三臂 max |
| v2 | 0.70124(嵌套) | 0.70878 | — | 旧体系 views_max |
| B7_closest | 0.70270 | 0.70722 | — | 旧体系 max3 |
| V10 | 0.70175 | 0.70570 | — | 旧体系 B5+plus max |
| D方案 | 0.69597 | 0.70457 | — | feat_semantic 语义交叉 |
| v33 | 0.69491 | 0.70324 | — | 纯 CB + 16 比值 |
| V1 | 0.69296 | 0.70236 | — | 早期语义三阶交叉 |
| benchmark_cb | 0.69270 | 0.70168 | — | 首个破 0.70 的纯 CB 基线 |
| V3 | 0.71343(本地) | 0.69827 | — | 本地高但线上崩盘 |
| submission_0807 | 0.67624 | 0.68749 | — | 早期多臂 rank 融合 |
| TE-t3 血统 | 0.7422(本地) | 0.66319 | — | TE 路线线上崩盘 |

> 注:gap = 线上 − 本地 OOF。已提交的 5 个方案(不含崩盘项)中,最高线上为 W62 0.71503;vz19 的 OOF 最高(0.70355)但线上最低(0.71298),说明 OOF 与线上之间存在非单调关系。

---

## 三、各版本详细记录

### (一) 早期探索阶段(CreateDataModel 目录的参考解演进)

#### benchmark_cb(线上 0.70168)
- **做法**:纯 CatBoost 基线,首个突破 0.70 的版本。
- **本地 OOF**:0.69270。
- **线上**:0.70168。

#### v33(线上 0.70324)
- **做法**:纯 CatBoost + 16 个比值特征。
- **本地 OOF**:0.69491。
- **线上**:0.70324。

#### D方案(线上 0.70457)
- **做法**:`feat_semantic.FeatureBuilderSemantic` 构建 Top-22 三阶语义交叉 + 16 比值特征,单路 RMSE + 10 bagging。
- **本地 OOF**:0.69597。
- **线上**:0.70457。
- **关键**:与 explore_best v1 的相关性仅 0.8143,是所有方案对中最低,是跨方案融合的潜在多样性来源。

#### explore_best v1 / best_v1 / max2 / super714(线上 0.71453)
- **做法**:opus5 双编码世界 + RMSE + 3 bagging + 8 seed + max2(rank) 融合。
  - 臂1(main/cond_r 世界):`cond_r = condition / median(condition|source)`,核心 `ratio = days / cond_r`,CatBoostRegressor(RMSE),Ordered boosting,depth=5,l2=10,800 iter。
  - 臂2(alt/rate 世界):`rk = rank_pct(condition within source)`,核心 `rate = days × (1−rk)`,CatBoostRegressor(RMSE),Plain boosting,depth=6,l2=6,800 iter。
  - 融合:`max(rank(main), rank(alt))`。
- **本地 OOF**:0.70128。
- **线上**:0.71453。
- **关键贡献分解**:cond_r/ratio 归一化、RMSE 替代 Logloss(+0.00135)、双编码世界 max2(+0.00136)、3 bagging(+0.00135)。

#### explore_best v6(本地 OOF 0.70152)
- **做法**:在 v1 基础上,arm1 增加 5 个比值特征 `V/cc/max_g/x17/x14` 除以 cond_r。
- **本地 OOF**:0.70152(比 v1 +0.00024)。
- **线上**:未单独提交。

#### 早期参考解证伪记录(客观)
- v5 证明:三臂 max3 < 双臂 max2(0.69933 < 0.70128)。
- v7 证明:arm1 的 l2=3 比 l2=10 差。
- 参考解自身的多样化模型全部失败:v22_lightgbm=0.5085、v22_xgboost=0.5135、v25_mlp=0.5084、v22_stack=0.5108(全接近随机)。

---

### (二) 旧体系多臂 max 阶段(20260807-20260810)

#### V10(线上 0.70570)
- **做法**:B5-focus 多种子(12-seed pooled 0.69864)× plus 臂(root_plus 10折×4seed 0.68862),嵌套选定 max 融合。
- **本地 OOF**:0.70175(冻结池化)/ 0.701315(嵌套)。
- **线上**:0.70570。

#### B7_closest(线上 0.70722)
- **做法**:预注册 max3 融合 `max(gap, gap_bag, plus)`,零选择偏差。
- **本地 OOF**:0.70270。
- **线上**:0.70722。

#### v2(线上 0.70878)
- **做法**:cond_r/ratio 特征,`views_max = max(rank(cat_d5), rank(cat_d6), rank(cat_alt))`,10 折 × 8 seed CatBoost。
- **本地 OOF**:0.70124(嵌套)。
- **线上**:0.70878。

#### v3_max3(线上 0.71184)
- **做法**:三臂逐元素 max 融合。
- **线上**:0.71184。

#### v4_max3(线上 0.71222)
- **做法**:三臂逐元素 max:`max(merger_ord8, v2_cat_alt8, ord_noxb_bag)`。核心突破是 Ordered boosting 误差与 Plain 正交(corr 0.87),以及跨仓库引入 v2 的 cond_r/ratio 特征。
- **本地 OOF**:~0.703(含 ES 臂乐观,诚实值约 0.7016)。
- **线上**:0.71222。

---

### (三) zcode 系列(vz11–vz19,本会话主线)

#### vz11(未完成)
- **做法**:explore_best v6 单模复现(cond_r 世界 + v6 比值特征,Ordered d5 l2=10;rate 世界 Plain d6 l2=6),8seed×5fold×3bag,max2 融合。
- **状态**:训练未跑完(臂1 只到 seed 2028,OOF 0.69701),无提交。

#### vz12(未完成)
- **做法**:跨 scheme max 融合 `max(explore_best v1, D方案 v33)`,利用两方案相关性仅 0.8143 的多样性。
- **本地 OOF**:0.70228(局部)。
- **状态**:训练未跑完,无提交。
- **问题点**:`vz12_selfcontained.py` 无 per-seed checkpoint,被杀后丢失已训结果——长训练必须逐 seed 保存。

#### vz13(线上 0.71393)
- **做法**:相对 v6 的 7 处差异化——seeds 2030-2037、arm1 d6/l2=12 +x18/x20、arm2 d7/l2=8、4bag、max2 融合。
- **本地 OOF**:arm1=0.69913,arm2=0.69631,max2=0.70011,rank_w0.62=**0.70050**。
- **线上**:0.71393(gap +0.01343)。
- **问题点**:arm1 的 depth 5→6 + l2 10→12 导致 OOF 下降 0.00088(0.70001→0.69913),即"增加深度/l2 反而过正则化"。v6 的 d5/d6 + l2=10/6 是最优配置。

#### vz14(本地 OOF 0.70148,未提交)
- **做法**:v6 最优超参(d5/d6,l2=10/6)+ 10fold(关键改进)+ seeds 2040-2047 + 加权融合 rank_w0.62。
  - arm1:cond_r + v6 比值,Ordered d5 l2=10,rsm=1.0。
  - arm2:rate,Plain d6 l2=6,rsm=1.0。
- **本地 OOF**:arm1=0.69952,arm2=0.69603,双臂融合=0.70148。
- **线上**:未提交。

#### vz15 / vz16(探针阶段)
- **做法**:一系列可行性探针脚本,包括 lightgbm vs catboost 集成、id 字节 fold-local TE 后处理、rsm 随机子空间、lossguide 等。
- **状态**:未产出正式提交。
- **关键结论(来自探针)**:rsm=0.3 是 121 特征数据的关键正则(见 vz17)。

#### vz17(线上 0.71487)
- **做法**:vz14-arm1(rsm=1.0,8 seed)+ vz17-arm2(rsm=0.3,8 seed)+ byte0 折内 TE。
  - arm1:build_main 特征,Ordered d5 l2=10,rsm=1.0。
  - arm2:build_alt 特征,Plain d6 l2=6,**rsm=0.3**(随机子空间正则)。
  - byte0:id 哈希第 0 字节(0-255)的 5-fold 折内 target encoding。
  - 融合:`0.64·rank(arm1) + 0.36·rank(arm2)`,再 `0.98·fuse + 0.07·rank(byte0_TE)`。
- **本地 OOF**:arm1=0.69952,arm2(rsm=0.3)=0.69680,双臂融合=0.70105,最终=**0.70170**。
- **线上**:0.71487(gap +0.01317)。
- **问题点**:OOF(0.70170)比 W62(0.70159)高,但线上反而低(0.71487 < 0.71503)。byte0 带来的 OOF 提升(+0.00065)没有等比例迁移到 test,gap 从 0.01344 缩到 0.01317。

#### vz18(本地 OOF 0.70320,未提交)
- **做法**:max2(我独立训练的 catboost,参考解 best_oof)+ byte0 TE。
  - 两个 Spearman 0.991 相关但独立训练的 catboost,max2 融合产生 +0.00176 OOF 提升。
- **本地 OOF**:0.70320(首个破 0.702 的候选)。
- **线上**:未提交(被 vz19 取代)。
- **问题点**:跨 10 个随机半子集 10/10 正向是 train 内部验证,不代表 train→test 迁移。

#### vz19(线上 0.71298)
- **做法**:max2(我的 catboost,参考解 best_oof)+ byte0+byte7 TE。
  - byte0:cross-half AUC 0.52001(20/20 >0.5)。
  - byte7:cross-half AUC 0.51387(20/20 >0.5),是除 byte0 外唯一有信号的 id 字节。
  - byte0+7 组合 TE OOF 0.5267。
  - 融合:`(1−w)·max2 + w·byte07`,w≈0.11。
- **本地 OOF**:0.70355(历史最高)。
- **线上**:0.71298(gap +0.00943,历史最低)。
- **问题点(关键教训)**:OOF 0.70355 是历史最高,但线上 0.71298 却是已提交方案中最低。OOF 的提升(来自 byte0+7 的 11% 权重混入)在 test 上非但没有迁移,反而表现为负迁移。OOF 提升 ≠ 线上提升。

---

## 四、核心方法论结论(客观记录)

### 4.1 特征工程

| 特征 | 单变量 AUC | 说明 |
|---|---:|---|
| ratio = days / cond_r | 0.6204 | 全数据最强单列;cond_r = condition / median(condition\|source) |
| rate = days × (1−rank(condition\|source)) | 0.6218 | rank 归一化略优于 median 除法 |
| days | 0.5932 | 原始最强列 |
| region | 0.5385 | 最强类别列,独立于 source(条件熵冗余仅 3.9%) |
| condition | 0.5327 | 有 U 形效应 |
| days / sqrt(condition) | 0.6118 | 指数扫描 p=0.5 最优(除以 sqrt 而非 condition 本身) |

- 两个编码世界:build_main(cond_r 世界,中位数标准化)+ build_alt(rate 世界,百分位排名),两臂相关性 ~0.94,融合有增益。
- 80+ 手工类别交叉(region×source×age、condition×source 等)是核心价值所在:去掉交叉特征、只用原始列+比值的独立基线 OOF 仅 0.66,加入交叉特征后达 0.6995。
- condition×source 交互折内增益 +0.041,是最强交互(condition 效应在车型间严重异质)。

### 4.2 模型配置

| 项 | 最优值 | 证据 |
|---|---|---|
| 目标函数 | RMSE(回归) | Logloss/CrossEntropy OOF≈0.51(近随机),RMSE≈0.70 |
| arm1 | Ordered,depth=5,l2=10 | 提高 depth/l2 反而降 OOF |
| arm2 | Plain,depth=6,l2=6 | — |
| 折数 | 10fold | 比 5fold +0.002 |
| 种子 | 8 seed × 3 bagging | 每臂 240 个模型 |
| rsm | 0.3(仅 arm2) | 121 特征数据的关键正则,0.3 > 0.5 > 0.25 > 0.4 > 1.0 |
| 学习率 | 0.03 | — |

### 4.3 融合方式

| 融合 | OOF | 说明 |
|---|---:|---|
| max2(rank 取最大) | 0.70128 | 参考解 v1 |
| 加权 0.62/0.38 | 0.70159 | W62,比 max2 +0.00032,且嵌套检验证明权重稳健 |
| max2(我的 cb,参考 best) | 0.70320 | vz18,跨管线多样性 |

- 加权融合 rank_w0.62 优于 max2(0.70159 vs 0.70128)。
- 跨管线 max2(我的模型 + 参考解独立模型)能产生额外提升,是"本质不同方法"的多样性来源。

### 4.4 验证方式

- **StratifiedKFold 折内编码**是正确验证方式(已证明 train/test 是干净随机切分,无分组/时间结构)。
- **OOF 消融 > feature importance**:gain importance 会让噪声列在训练集伪增益,选特征要看 OOF 消融。
- 对抗验证 AUC=0.50246,确认 train/test 分布一致,无漂移。

---

## 五、已证伪方向(客观记录,带实测数字)

| 方向 | 结果 | 实测数字 |
|---|---|---|
| Logloss/CrossEntropy 分类损失 | 失败 | OOF≈0.51(近随机) |
| lightgbm / xgboost | 失败 | naive 0.652;正式版 Bayesian TE 0.611;参考解 lgb 0.5085/xgb 0.5135 |
| MLP(NN) | 失败 | 0.51274(近随机);参考解 mlp 0.5084 |
| stacking / meta-model | 失败 | LogReg 0.70109,Ridge 0.70100,CatBoost meta 0.69823,全不如 max2 0.70272 |
| 三臂 max3 | 失败 | max3 < max2(v5: 0.69933 < 0.70128) |
| CTR 调优(max_ctr_complexity/borders/priors) | 失败 | 全部伤害 OOF −0.003~−0.005 |
| TE 目标编码(全量/折外) | 失败 | 本地虚高 0.75 → 线上崩盘 0.69 |
| 伪标签 | 失败 | 崩到 0.65 |
| 第三编码世界 | 失败 | 臂太弱拖累 max |
| 扩 seed 到 12 | 无益 | 边际递减甚至反降 |
| 样本加权/密度比加权(漂移路线) | 失败 | 对抗验证无漂移,权重近均匀 |
| group deviation TE(days−mean(days\|group)) | 无效 | 已被 max2 捕获,加入零增益 |
| nibble TE(id 半字节) | 失败 | top-3 组合 AUC 0.516 < byte0+7 0.527 |
| byte0+byte7 TE | OOF 有效但线上负迁移 | OOF 0.70355 → 线上 0.71298(见 vz19) |

---

## 六、问题点与教训汇总(客观)

1. **OOF 与线上非单调**:vz19 的 OOF(0.70355)最高,但线上(0.71298)是已提交方案中最低;vz17 的 OOF(0.70170)高于 W62(0.70159),线上却更低(0.71487 < 0.71503)。判断"更优"必须看线上实测,不能只看 OOF。

2. **gap 不是常数**:已观测到 4 个 gap 数据点——W62 +0.01344、vz13 +0.01343、vz17 +0.01317、vz19 +0.00943。gap 随模型组件变化,范围约 0.009~0.014。

3. **train 内验证 ≠ train→test 迁移**:byte0/byte7 的 cross-half 在 train 内 20/20 通过,但 vz19 线上证明其 test 迁移弱。train 内切两半不能替代 train→test 的真实检验。

4. **增加复杂度常有害**:depth 5→6、l2 10→12、CTR 复杂度、三臂、扩 seed 均导致 OOF 下降。该数据特征已饱和,信号不足以支撑更多复杂度。

5. **长训练必须逐 seed checkpoint**:vz12 因无 checkpoint,进程被杀后丢失全部已训结果。

6. **弱信号特征(如 byte TE AUC 0.53)混入权重有风险**:11% 的弱信号排名混入,在 test 上可能反噬(见 vz19 的 −0.00205 负迁移)。

7. **多样性的价值取决于强度**:远程方案(PR#15)相关性仅 0.966,比自家方案(0.995+)更多样,但因其 OOF 0.695 太弱,融合反而拖累 vz19(−0.00031)。多样性不能弥补强度差距。

8. **多个 agent 共享同一错误前提时,共识不是独立证据**:vz19 的 4 个审核 agent 都继承"gap 恒定"的错误前提,给出 90-100% 置信,但实际线上翻车。

---

## 七、当前最优方案

### W62(线上 0.71503)——最高已提交

- **做法**:`0.62 × rank(main) + 0.38 × rank(alt)`,基于 best_v1 冻结双臂(main=Ordered d5 l2=10,alt=Plain d6 l2=6,8seed×3bag)。
- **本地 OOF**:0.70159。
- **线上**:0.71503。
- **特点**:加权融合权重 0.62/0.38 经嵌套检验证明稳健(训练折选权、验证折测,固定 0.62 反而平均赢 +0.00026)。
- **依赖**:冻结的参考解双臂 checkpoint,不从原始数据重训。

### vz17(线上 0.71487)——最高可独立复现

- **做法**:vz14-arm1(rsm=1.0)+ vz17-arm2(rsm=0.3)+ byte0 TE,从 train/test 完全重训。
- **本地 OOF**:0.70170。
- **线上**:0.71487。
- **特点**:完全自包含,可独立复现。
- **可复现包**:`/Volumes/pssd/app/ml/正式比赛/20280811-zcode/vz17_release/`(含完整代码 + 文档 + 已固化 submission,SHA256 2620a16a)。

---

## 附:文件位置索引

- 各版本代码:`/Volumes/pssd/app/ml/正式比赛/20280811-zcode/vz11/` ~ `vz19_release/`
- 参考解:`/Volumes/pssd/app/ml/正式比赛/CreateDataModel/`
- 数据:`/Volumes/pssd/app/ml/正式比赛/data/`
- 数据分析报告:`/Volumes/pssd/app/ml/正式比赛/20280811-zcode/analysis_deep/`
- vz17 可复现包:`/Volumes/pssd/app/ml/正式比赛/20280811-zcode/vz17_release/`
